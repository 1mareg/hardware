<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HardwareApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/background_color_button</item>
    </style>

    <style name="Theme.HardwareApplication" parent="Base.Theme.HardwareApplication" />

    <style name="ButtonStyle">
        <item name="android:textSize">@dimen/text_size_button</item>
        <item name="android:background">@color/background_color_button</item>
    </style>
    <style name="BlockHeaderStyle">
        <item name="android:textSize">@dimen/text_size_block_head</item>
        <item name="android:textColor">@color/text_color_block_head</item>
    </style>
</resources>